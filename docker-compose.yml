version: '3.8'

services:
  portfolio:
    build: .
    container_name: phill-portfolio
    ports:
      - "90:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      # Optional: mount logs for debugging
      - ./logs:/var/log/nginx
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge 